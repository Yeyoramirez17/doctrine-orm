# Doctrine ORM

Este proyecto es un ejemplo práctico de cómo usar Doctrine ORM en PHP. Doctrine ORM es una biblioteca de mapeo objeto-relacional (ORM) que permite a los desarrolladores trabajar con bases de datos utilizando objetos en lugar de escribir consultas SQL directamente.
